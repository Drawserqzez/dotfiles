local kanagawa = require('kanagawa')

kanagawa.setup({
    dimInactive = false,
    terminalColours = true,
    colors = {
        theme = {
            all = {
                ui = {
                    bg_gutter = "none"
                }
            }
        }
    }
})

-- "vim.cmd('colorscheme kanagawa-wave')

-- kanagawa.load("wave")
